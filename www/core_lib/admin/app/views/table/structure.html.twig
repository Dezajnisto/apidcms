{# twig #}
{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block page_title %}{{ title }}{% endblock %}

{% block content %}
<div class="max-w-6xl mx-auto">
    <!-- Навигация (responsive) -->
    <div class="mb-4 md:mb-6 flex flex-wrap items-center gap-3">
        <a href="{{ admin_url('table/' ~ tableName|url_encode) }}" 
           class="inline-flex items-center space-x-2 text-blue-600 hover:text-blue-800 transition text-sm">
            <i class="fas fa-arrow-left"></i>
            <span>{{ lang('table.struct.back_to_data') }}</span>
        </a>
        <a href="{{ admin_url('') }}" 
           class="inline-flex items-center space-x-2 text-gray-600 hover:text-gray-800 transition text-sm">
            <i class="fas fa-table"></i>
            <span>{{ lang('table.struct.table_list') }}</span>
        </a>
    </div>

    <!-- Заголовок -->
    <div class="mb-4 md:mb-6">
        <h1 class="text-xl md:text-2xl font-bold text-gray-900">{{ title }}</h1>
        <p class="text-sm text-gray-600 mt-1">{{ lang('table.struct.subtitle') }}</p>
    </div>

    <!-- Сообщения -->
    {% if _GET.column_added == '1' %}
        <div class="mb-4 md:mb-6 p-3 md:p-4 bg-green-100 border border-green-400 text-green-700 rounded-lg text-sm">
            <div class="flex items-center space-x-3">
                <i class="fas fa-check-circle"></i>
                <span>{{ lang('table.struct.column_added') }}</span>
            </div>
        </div>
    {% endif %}
    {% if _GET.column_deleted == '1' %}
        <div class="mb-4 md:mb-6 p-3 md:p-4 bg-blue-100 border border-blue-400 text-blue-700 rounded-lg text-sm">
            <div class="flex items-center space-x-3">
                <i class="fas fa-info-circle"></i>
                <span>{{ lang('table.struct.column_deleted') }}</span>
            </div>
        </div>
    {% endif %}

    <!-- {{ lang('table.struct.actions') }} -->
    <div class="mb-4 md:mb-6 flex flex-wrap gap-2 md:gap-4">
        <a href="{{ admin_url('table/' ~ tableName|url_encode ~ '/add-column') }}" 
           class="inline-flex items-center space-x-2 px-3 md:px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700 transition text-sm">
            <i class="fas fa-plus"></i>
            <span>{{ lang('table.struct.add_column') }}</span>
        </a>
        <a href="{{ admin_url('table/' ~ tableName|url_encode ~ '/delete-table') }}" 
           class="inline-flex items-center space-x-2 px-3 md:px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-700 transition text-sm"
           onclick="return confirm('{{ lang('table.struct.confirm_delete_table') }}')">
            <i class="fas fa-trash"></i>
            <span>{{ lang('table.struct.delete_table') }}</span>
        </a>
    </div>

    <!-- Структура -->
    <div class="bg-white rounded-lg shadow-sm border">
        <div class="px-4 md:px-6 py-3 md:py-4 border-b">
            <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-2">
                <h2 class="text-base md:text-lg font-semibold text-gray-900">{{ lang('table.struct.title') }}</h2>
                <div class="flex items-center space-x-3 text-xs md:text-sm text-gray-500">
                    <span class="flex items-center space-x-1">
                        <i class="fas fa-columns"></i>
                        <span>{{ structure|length }} {{ lang('table.struct.columns_short') }}</span>
                    </span>
                    <span class="flex items-center space-x-1">
                        <i class="fas fa-key"></i>
                        <span>{{ structure|filter(col => col.pk)|length }} {{ lang('table.struct.keys_short') }}</span>
                    </span>
                </div>
            </div>
        </div>
        
        <div class="p-4 md:p-6">
            {% if structure is empty %}
                <div class="text-center py-8 md:py-12">
                    <i class="fas fa-columns text-gray-400 text-4xl md:text-5xl mb-4"></i>
                    <p class="text-gray-500 text-base md:text-lg mb-2">{{ lang('table.struct.no_columns') }}</p>
                    <p class="text-gray-400 text-sm mb-6">{{ lang('table.struct.add_first_hint') }}</p>
                    <a href="{{ admin_url('table/' ~ tableName|url_encode ~ '/add-column') }}" 
                       class="inline-flex items-center space-x-2 px-4 md:px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 transition text-sm">
                        <i class="fas fa-plus"></i>
                        <span>{{ lang('table.struct.add_first') }}</span>
                    </a>
                </div>
            {% else %}
                <!-- Desktop table + mobile cards via data-table -->
                <div class="overflow-x-auto">
                    <table class="data-table min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.name') }}</th>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.type') }}</th>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.null') }}</th>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.pk') }}</th>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.default_short') }}</th>
                                <th class="px-4 md:px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{ lang('table.struct.actions') }}</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            {% for column in structure %}
                            <tr class="hover:bg-gray-50 transition">
                                <td class="px-4 md:px-6 py-4 text-sm font-medium text-gray-900" data-label="{{ lang('table.struct.name') }}">
                                    <div class="flex items-center space-x-3">
                                        <i class="fas fa-columns text-gray-400"></i>
                                        <span>{{ column.name }}</span>
                                        {% if column.name == 'id' %}
                                            <span class="ml-1 inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">{{ lang('table.struct.pk') }}</span>
                                        {% endif %}
                                    </div>
                                </td>
                                <td class="px-4 md:px-6 py-4 text-sm" data-label="{{ lang('table.struct.type') }}">
                                    <code class="bg-gray-100 px-2 py-1 rounded text-xs">{{ column.type }}</code>
                                </td>
                                <td class="px-4 md:px-6 py-4" data-label="{{ lang('table.struct.null') }}">
                                    {% if column.notnull %}
                                        <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                                            <i class="fas fa-times mr-1"></i>{{ lang('table.struct.no') }}
                                        </span>
                                    {% else %}
                                        <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                            <i class="fas fa-check mr-1"></i>{{ lang('table.struct.yes') }}
                                        </span>
                                    {% endif %}
                                </td>
                                <td class="px-4 md:px-6 py-4" data-label="{{ lang('table.struct.pk') }}">
                                    {% if column.pk %}
                                        <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800">
                                            <i class="fas fa-key mr-1"></i>{{ lang('table.struct.pk') }}
                                        </span>
                                    {% else %}
                                        <span class="text-gray-400 text-sm">—</span>
                                    {% endif %}
                                </td>
                                <td class="px-4 md:px-6 py-4 text-sm" data-label="{{ lang('table.struct.default_short') }}">
                                    {% if column.dflt_value %}
                                        <code class="bg-gray-100 px-2 py-1 rounded text-xs">{{ column.dflt_value }}</code>
                                    {% else %}
                                        <span class="text-gray-400">{{ lang('table.struct.null') }}</span>
                                    {% endif %}
                                </td>
                                <td class="px-4 md:px-6 py-4" data-label="{{ lang('table.struct.actions') }}">
                                    {% if not column.pk and column.name != 'id' %}
                                        <a href="{{ admin_url('table/' ~ tableName|url_encode ~ '/delete-column/' ~ column.name|url_encode) }}" 
                                           class="inline-flex items-center space-x-1 px-2 md:px-3 py-1 border border-red-300 text-red-700 rounded-md hover:bg-red-50 transition text-xs"
                                           onclick="return confirm('{{ lang('table.struct.confirm_delete_column') }}')">
                                            <i class="fas fa-trash"></i>
                                            <span class="hidden md:inline ml-1">{{ lang('table.struct.delete') }}</span>
                                        </a>
                                    {% else %}
                                        <span class="inline-flex items-center space-x-1 px-2 md:px-3 py-1 border border-gray-300 text-gray-400 rounded-md cursor-not-allowed text-xs">
                                            <i class="fas fa-lock"></i>
                                            <span class="hidden md:inline ml-1">{{ lang('table.struct.system') }}</span>
                                        </span>
                                    {% endif %}
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endif %}
        </div>
    </div>

    <!-- Статистика таблицы -->
    {% if structure is not empty %}
    <div class="mt-4 md:mt-6 grid grid-cols-2 sm:grid-cols-4 gap-3 md:gap-4">
        <div class="bg-blue-50 p-3 md:p-4 rounded-lg border border-blue-200">
            <div class="flex items-center space-x-3">
                <i class="fas fa-columns text-blue-500"></i>
                <div>
                    <p class="text-lg md:text-2xl font-bold text-blue-700">{{ structure|length }}</p>
                    <p class="text-xs text-blue-600">{{ lang('table.struct.total_columns') }}</p>
                </div>
            </div>
        </div>
        <div class="bg-purple-50 p-3 md:p-4 rounded-lg border border-purple-200">
            <div class="flex items-center space-x-3">
                <i class="fas fa-key text-purple-500"></i>
                <div>
                    <p class="text-lg md:text-2xl font-bold text-purple-700">{{ structure|filter(col => col.pk)|length }}</p>
                    <p class="text-xs text-purple-600">{{ lang('table.struct.primary_keys') }}</p>
                </div>
            </div>
        </div>
        <div class="bg-green-50 p-3 md:p-4 rounded-lg border border-green-200">
            <div class="flex items-center space-x-3">
                <i class="fas fa-check-circle text-green-500"></i>
                <div>
                    <p class="text-lg md:text-2xl font-bold text-green-700">{{ structure|filter(col => not col.notnull)|length }}</p>
                    <p class="text-xs text-green-600">{{ lang('table.struct.null') }} колонок</p>
                </div>
            </div>
        </div>
        <div class="bg-orange-50 p-3 md:p-4 rounded-lg border border-orange-200">
            <div class="flex items-center space-x-3">
                <i class="fas fa-edit text-orange-500"></i>
                <div>
                    <p class="text-lg md:text-2xl font-bold text-orange-700">{{ structure|filter(col => not col.pk and col.name != 'id')|length }}</p>
                    <p class="text-xs text-orange-600">{{ lang('table.struct.editable') }}</p>
                </div>
            </div>
        </div>
    </div>

    <!-- SQL и info -->
    <div class="mt-4 md:mt-6 space-y-4">
        <div class="bg-white rounded-lg border border-gray-200 overflow-hidden">
            <div class="px-4 md:px-6 py-3 md:py-4 border-b border-gray-200 bg-gray-50">
                <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3">
                    <div class="flex items-center space-x-3">
                        <i class="fas fa-code text-gray-600"></i>
                        <h3 class="text-base md:text-lg font-semibold text-gray-900">{{ lang('table.struct.sql_title') }}</h3>
                    </div>
                    <button onclick="copySqlStructure(event)" 
                            class="inline-flex items-center space-x-2 px-3 md:px-4 py-2 bg-blue-600 text-white text-xs md:text-sm rounded-md hover:bg-blue-700 transition">
                        <i class="far fa-copy"></i>
                        <span>{{ lang('table.struct.copy_sql') }}</span>
                    </button>
                </div>
            </div>
            <div class="p-4 md:p-6 bg-gray-900 text-gray-100 overflow-x-auto">
                <pre class="text-xs md:text-sm font-mono whitespace-pre"><code id="sqlStructure">CREATE TABLE "{{ tableName }}" ({{ "\n" }}{% for column in structure %}    "{{ column.name }}" {{ column.type }}{% if column.notnull == 0 %} {{ lang('table.struct.null') }}{% endif %}{% if column.dflt_value is not null %} DEFAULT {{ column.dflt_value }}{% endif %}{% if column.pk %} PRIMARY KEY{% endif %}{% if not loop.last %},{% endif %}{{ "\n" }}{% endfor %});</code></pre>
            </div>
        </div>

        <div class="p-3 md:p-4 bg-gray-50 rounded-lg border text-sm">
            <div class="flex items-start space-x-3">
                <i class="fas fa-info-circle text-gray-500 mt-1"></i>
                <div class="text-gray-700">
                    <strong class="font-medium">{{ lang('table.struct.table_info') }}</strong>
                    <div class="mt-2 grid grid-cols-1 sm:grid-cols-2 gap-2 text-xs md:text-sm">
                        <div class="flex items-center space-x-2">
                            <i class="fas fa-table"></i>
                            <span>{{ lang('table.struct.name') }}: <code class="bg-gray-200 px-1 rounded">{{ tableName }}</code></span>
                        </div>
                        <div class="flex items-center space-x-2">
                            <i class="fas fa-database"></i>
                            <span>{{ lang('table.struct.db_label') }}: <code class="bg-gray-200 px-1 rounded">SQLite</code></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% endif %}
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.createElement('input');
    searchInput.type = 'text';
    searchInput.placeholder = '{{ lang('table.struct.search_placeholder')|e('js') }}';
    searchInput.className = 'w-full px-3 md:px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 mb-4 text-sm';
    
    const table = document.querySelector('table');
    if (table) {
        table.parentNode.insertBefore(searchInput, table);
        
        searchInput.addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            const rows = table.querySelectorAll('tbody tr');
            rows.forEach(row => {
                const nameEl = row.querySelector('td:first-child .font-medium, td:first-child span');
                const columnName = nameEl ? nameEl.textContent.toLowerCase() : '';
                row.style.display = columnName.includes(searchTerm) ? '' : 'none';
            });
        });
    }
});

function copySqlStructure(event) {
    const sqlText = document.getElementById('sqlStructure').textContent;
    const textarea = document.createElement('textarea');
    textarea.value = sqlText;
    document.body.appendChild(textarea);
    textarea.select();
    textarea.setSelectionRange(0, 99999);
    
    try {
        const successful = document.execCommand('copy');
        const button = event.target.closest('button');
        
        if (successful) {
            const originalHtml = button.innerHTML;
            button.innerHTML = '<i class="fas fa-check"></i><span>{{ lang('table.struct.copied')|e('js') }}</span>';
            button.classList.remove('bg-blue-600', 'hover:bg-blue-700');
            button.classList.add('bg-green-600', 'hover:bg-green-700');
            
            setTimeout(() => {
                button.innerHTML = originalHtml;
                button.classList.remove('bg-green-600', 'hover:bg-green-700');
                button.classList.add('bg-blue-600', 'hover:bg-blue-700');
            }, 2000);
        }
    } catch (err) {
        console.error('{{ lang('table.struct.copy_error')|e('js') }}: ', err);
    }
    
    document.body.removeChild(textarea);
}
</script>
{% endblock %}